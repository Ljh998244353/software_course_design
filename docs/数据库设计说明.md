# 在线拍卖平台数据库设计说明

## 1. 文档目的

本文档用于完成 `S03`，在现有需求规格和概要设计的基础上，给出本项目在课程设计阶段的数据库物理设计建议。

本步重点解决以下问题：

- 核心表如何落到 MySQL 8
- 关键状态字段如何定义
- 唯一约束和索引如何支撑主业务流程
- 初始化脚本如何组织
- 初始化数据和迁移策略如何规划

本文档是后续 `S04-S11` 模块设计与实现的数据库基线。

## 2. 设计约束

数据库设计遵循以下约束：

- 事务型事实数据统一落 MySQL，Redis 仅作缓存和热点读模型
- 课程设计阶段优先保证正确性、可验证性和可演示性，不引入分库分表
- 关键业务主链路必须能支撑用户注册登录、拍品发布审核、活动创建、出价竞拍、成交支付、评价反馈
- 拍卖、出价、订单、支付等关键状态必须可追踪
- 幂等、审计、补偿所需字段和日志表必须预留

## 3. 物理设计基线

### 3.1 数据库与字符集

建议基线如下：

- 数据库：MySQL 8
- 存储引擎：InnoDB
- 字符集：`utf8mb4`
- 排序规则：`utf8mb4_0900_ai_ci`
- 时间字段：优先使用 `datetime(3)`
- 金额字段：统一使用 `decimal(12,2)`
- 主键：统一使用 `bigint unsigned`

### 3.2 命名约定

建议约定如下：

- 表名使用小写下划线风格
- 主键统一为 `{table}_id` 或明确业务主键名
- 业务编号字段使用 `*_no`
- 创建时间统一为 `created_at`
- 更新时间统一为 `updated_at`
- 逻辑状态字段统一使用 `status`

说明：

- 概要设计中的 `User` 实体在物理表中建议使用 `user_account`，避免与系统库对象混淆
- 概要设计中的 `Order` 实体保持使用 `order_info`

## 4. 实体与表映射

| 领域对象 | 物理表 | 说明 |
|---|---|---|
| User | `user_account` | 用户账号与平台权限 |
| ItemCategory | `item_category` | 拍品分类字典 |
| Item | `item` | 拍品基础信息 |
| ItemImage | `item_image` | 拍品图片 |
| ItemAuditLog | `item_audit_log` | 审核轨迹 |
| Auction | `auction` | 拍卖活动 |
| Bid | `bid_record` | 出价记录 |
| Order | `order_info` | 成交订单 |
| Payment | `payment_record` | 支付流水 |
| PaymentCallbackLog | `payment_callback_log` | 支付回调日志 |
| Review | `review` | 评价记录 |
| Notification | `notification` | 站内通知 |
| StatisticsDaily | `statistics_daily` | 日统计报表 |
| TaskLog | `task_log` | 调度与补偿任务日志 |
| OperationLog | `operation_log` | 运维与敏感操作日志 |

## 5. 核心表设计

### 5.1 `user_account`

用途：

- 存储用户账号、密码摘要、平台角色和账号状态
- 普通用户统一用一个账号承载买家和卖家行为，不拆成两套身份表

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| user_id | bigint unsigned | PK, auto_increment | 用户主键 |
| username | varchar(50) | not null, unique | 用户名 |
| password_hash | varchar(255) | not null | 密码哈希 |
| phone | varchar(20) | unique | 手机号 |
| email | varchar(100) | unique | 邮箱 |
| role_code | varchar(20) | not null | 平台角色 |
| status | varchar(20) | not null | 账号状态 |
| nickname | varchar(50) |  | 昵称 |
| last_login_at | datetime(3) |  | 最后登录时间 |
| created_at | datetime(3) | not null | 创建时间 |
| updated_at | datetime(3) | not null | 更新时间 |

状态建议：

- `role_code`：`USER`、`ADMIN`、`SUPPORT`
- `status`：`ACTIVE`、`FROZEN`、`DISABLED`

索引建议：

- `uk_user_account_username(username)`
- `uk_user_account_phone(phone)`
- `uk_user_account_email(email)`
- `idx_user_account_role_status(role_code, status)`

### 5.2 `item_category`

用途：

- 管理拍品分类，支撑发布页选择和管理端统计口径

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| category_id | bigint unsigned | PK, auto_increment | 分类主键 |
| category_name | varchar(64) | not null, unique | 分类名称 |
| status | varchar(20) | not null | 分类状态 |
| sort_no | int | default 0 | 排序号 |
| created_at | datetime(3) | not null | 创建时间 |
| updated_at | datetime(3) | not null | 更新时间 |

### 5.3 `item`

用途：

- 存储拍品主体信息
- 通过状态控制拍品从草稿、审核、待拍卖到成交或流拍的生命周期

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| item_id | bigint unsigned | PK, auto_increment | 拍品主键 |
| seller_id | bigint unsigned | not null, FK -> user_account.user_id | 卖家 |
| category_id | bigint unsigned | FK -> item_category.category_id | 分类 |
| title | varchar(200) | not null | 标题 |
| description | text | not null | 描述 |
| start_price | decimal(12,2) | not null | 建议起拍价 |
| item_status | varchar(32) | not null | 拍品状态 |
| reject_reason | varchar(255) |  | 驳回原因 |
| cover_image_url | varchar(255) |  | 封面图 |
| created_at | datetime(3) | not null | 创建时间 |
| updated_at | datetime(3) | not null | 更新时间 |

状态建议：

- `item_status`：`DRAFT`、`PENDING_AUDIT`、`REJECTED`、`READY_FOR_AUCTION`、`IN_AUCTION`、`SOLD`、`UNSOLD`、`OFFLINE`

索引建议：

- `idx_item_seller_status_created(seller_id, item_status, created_at)`
- `idx_item_category_status(category_id, item_status)`

### 5.4 `item_image`

用途：

- 存储拍品图片列表和展示顺序

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| image_id | bigint unsigned | PK, auto_increment | 图片主键 |
| item_id | bigint unsigned | not null, FK -> item.item_id | 拍品主键 |
| image_url | varchar(255) | not null | 图片地址 |
| sort_no | int | not null, default 0 | 排序 |
| is_cover | tinyint(1) | not null, default 0 | 是否封面 |
| created_at | datetime(3) | not null | 创建时间 |

约束建议：

- `uk_item_image_item_sort(item_id, sort_no)`

### 5.5 `item_audit_log`

用途：

- 记录管理员审核行为，满足可追踪与复核要求

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| audit_log_id | bigint unsigned | PK, auto_increment | 审核日志主键 |
| item_id | bigint unsigned | not null, FK -> item.item_id | 拍品 |
| admin_id | bigint unsigned | not null, FK -> user_account.user_id | 审核管理员 |
| audit_result | varchar(20) | not null | 审核结果 |
| audit_comment | varchar(255) |  | 审核意见 |
| created_at | datetime(3) | not null | 审核时间 |

状态建议：

- `audit_result`：`APPROVED`、`REJECTED`

索引建议：

- `idx_item_audit_log_item_created(item_id, created_at)`
- `idx_item_audit_log_admin_created(admin_id, created_at)`

### 5.6 `auction`

用途：

- 存储拍卖活动主状态
- 是竞价一致性的核心聚合根

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| auction_id | bigint unsigned | PK, auto_increment | 活动主键 |
| item_id | bigint unsigned | not null, FK -> item.item_id | 关联拍品 |
| seller_id | bigint unsigned | not null, FK -> user_account.user_id | 卖家 |
| start_time | datetime(3) | not null | 开始时间 |
| end_time | datetime(3) | not null | 结束时间 |
| start_price | decimal(12,2) | not null | 起拍价 |
| current_price | decimal(12,2) | not null | 当前最高价 |
| bid_step | decimal(12,2) | not null | 最小加价幅度 |
| highest_bidder_id | bigint unsigned | FK -> user_account.user_id | 当前最高出价者 |
| status | varchar(20) | not null | 活动状态 |
| anti_sniping_window_seconds | int | not null, default 0 | 触发延时保护的窗口秒数 |
| extend_seconds | int | not null, default 0 | 每次延长秒数 |
| version | bigint unsigned | not null, default 0 | 乐观锁版本号 |
| created_at | datetime(3) | not null | 创建时间 |
| updated_at | datetime(3) | not null | 更新时间 |

状态建议：

- `status`：`PENDING_START`、`RUNNING`、`SETTLING`、`SOLD`、`UNSOLD`、`CANCELLED`

说明：

- 为支持支付前结算控制，建议使用 `SETTLING` 作为拍卖结束到订单完成创建之间的过渡状态
- 同一拍品是否允许再次发起拍卖由业务规则决定，因此不对 `item_id` 做全局唯一约束
- 如需限制同一拍品同时仅存在一个非终态活动，建议在应用层事务里校验；MySQL 8 不适合用局部唯一索引直接表达该规则

索引建议：

- `idx_auction_status_start_end(status, start_time, end_time)`
- `idx_auction_item_status(item_id, status)`
- `idx_auction_seller_status(seller_id, status)`
- `idx_auction_highest_bidder(highest_bidder_id)`

### 5.7 `bid_record`

用途：

- 记录所有有效出价
- 作为竞价历史和一致性审计凭据

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| bid_id | bigint unsigned | PK, auto_increment | 出价主键 |
| auction_id | bigint unsigned | not null, FK -> auction.auction_id | 活动 |
| bidder_id | bigint unsigned | not null, FK -> user_account.user_id | 出价人 |
| request_id | varchar(64) | not null | 客户端幂等请求号 |
| bid_amount | decimal(12,2) | not null | 出价金额 |
| bid_status | varchar(20) | not null | 出价状态 |
| bid_time | datetime(3) | not null | 出价时间 |
| created_at | datetime(3) | not null | 记录创建时间 |

状态建议：

- `bid_status`：`WINNING`、`OUTBID`

关键约束：

- `uk_bid_record_request(auction_id, bidder_id, request_id)`

索引建议：

- `idx_bid_record_auction_created(auction_id, created_at)`
- `idx_bid_record_auction_bidder_created(auction_id, bidder_id, created_at)`

说明：

- 为保持历史清晰，建议只持久化有效出价，非法出价直接返回错误，不写入 `bid_record`
- 新最高价产生时，上一条最高出价记录应在同一事务中从 `WINNING` 更新为 `OUTBID`

### 5.8 `order_info`

用途：

- 存储拍卖成交后的订单主信息
- 保证一场拍卖最多只生成一笔订单

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| order_id | bigint unsigned | PK, auto_increment | 订单主键 |
| order_no | varchar(64) | not null, unique | 订单号 |
| auction_id | bigint unsigned | not null, unique, FK -> auction.auction_id | 拍卖活动 |
| buyer_id | bigint unsigned | not null, FK -> user_account.user_id | 买家 |
| seller_id | bigint unsigned | not null, FK -> user_account.user_id | 卖家 |
| final_amount | decimal(12,2) | not null | 成交金额 |
| order_status | varchar(20) | not null | 订单状态 |
| pay_deadline_at | datetime(3) |  | 支付截止时间 |
| paid_at | datetime(3) |  | 支付完成时间 |
| shipped_at | datetime(3) |  | 发货时间 |
| completed_at | datetime(3) |  | 交易完成时间 |
| closed_at | datetime(3) |  | 订单关闭时间 |
| created_at | datetime(3) | not null | 创建时间 |
| updated_at | datetime(3) | not null | 更新时间 |

状态建议：

- `order_status`：`PENDING_PAYMENT`、`PAID`、`WAITING_DELIVERY`、`SHIPPED`、`COMPLETED`、`REVIEWED`、`CLOSED`

索引建议：

- `uk_order_info_order_no(order_no)`
- `uk_order_info_auction(auction_id)`
- `idx_order_info_buyer_status(buyer_id, order_status)`
- `idx_order_info_seller_status(seller_id, order_status)`

### 5.9 `payment_record`

用途：

- 存储支付发起与最终支付结果
- 支撑支付状态追踪与金额校验

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| payment_id | bigint unsigned | PK, auto_increment | 支付记录主键 |
| payment_no | varchar(64) | not null, unique | 平台支付单号 |
| order_id | bigint unsigned | not null, FK -> order_info.order_id | 关联订单 |
| pay_channel | varchar(20) | not null | 支付渠道 |
| transaction_no | varchar(64) | unique | 第三方流水号 |
| pay_amount | decimal(12,2) | not null | 支付金额 |
| pay_status | varchar(20) | not null | 支付状态 |
| callback_payload_hash | varchar(128) |  | 回调摘要 |
| paid_at | datetime(3) |  | 支付完成时间 |
| created_at | datetime(3) | not null | 创建时间 |
| updated_at | datetime(3) | not null | 更新时间 |

状态建议：

- `pay_status`：`INITIATED`、`WAITING_CALLBACK`、`SUCCESS`、`FAILED`、`CLOSED`

索引建议：

- `uk_payment_record_payment_no(payment_no)`
- `uk_payment_record_transaction_no(transaction_no)`
- `idx_payment_record_order_status(order_id, pay_status)`

### 5.10 `payment_callback_log`

用途：

- 记录支付平台的每一次回调
- 支撑幂等判断、问题排查和人工补单

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| callback_log_id | bigint unsigned | PK, auto_increment | 回调日志主键 |
| payment_id | bigint unsigned | FK -> payment_record.payment_id | 支付记录 |
| order_id | bigint unsigned | FK -> order_info.order_id | 订单 |
| transaction_no | varchar(64) |  | 第三方流水号 |
| callback_status | varchar(20) | not null | 回调解析结果 |
| raw_payload | longtext | not null | 原始报文 |
| received_at | datetime(3) | not null | 接收时间 |
| processed_at | datetime(3) |  | 处理完成时间 |
| process_result | varchar(255) |  | 处理说明 |

状态建议：

- `callback_status`：`RECEIVED`、`VERIFIED`、`REJECTED`、`PROCESSED`

索引建议：

- `idx_payment_callback_log_tx_no(transaction_no, received_at)`
- `idx_payment_callback_log_order_received(order_id, received_at)`

### 5.11 `review`

用途：

- 存储买卖双方互评记录

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| review_id | bigint unsigned | PK, auto_increment | 评价主键 |
| order_id | bigint unsigned | not null, FK -> order_info.order_id | 订单 |
| reviewer_id | bigint unsigned | not null, FK -> user_account.user_id | 评价人 |
| reviewee_id | bigint unsigned | not null, FK -> user_account.user_id | 被评价人 |
| review_type | varchar(32) | not null | 评价方向 |
| rating | tinyint unsigned | not null | 评分 |
| content | varchar(500) |  | 评价内容 |
| created_at | datetime(3) | not null | 创建时间 |

约束建议：

- `uk_review_order_reviewer(order_id, reviewer_id)`

状态建议：

- `review_type`：`BUYER_TO_SELLER`、`SELLER_TO_BUYER`

### 5.12 `notification`

用途：

- 站内通知持久化，保证通知失败不影响主交易流程

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| notification_id | bigint unsigned | PK, auto_increment | 通知主键 |
| user_id | bigint unsigned | not null, FK -> user_account.user_id | 接收人 |
| notice_type | varchar(32) | not null | 通知类型 |
| title | varchar(100) | not null | 标题 |
| content | varchar(1000) | not null | 内容 |
| biz_type | varchar(32) |  | 业务类型 |
| biz_id | bigint unsigned |  | 业务对象 ID |
| read_status | varchar(20) | not null | 已读状态 |
| push_status | varchar(20) | not null | 推送状态 |
| created_at | datetime(3) | not null | 创建时间 |
| read_at | datetime(3) |  | 阅读时间 |

状态建议：

- `read_status`：`UNREAD`、`READ`
- `push_status`：`PENDING`、`SENT`、`FAILED`

索引建议：

- `idx_notification_user_read_created(user_id, read_status, created_at)`
- `idx_notification_biz_type_id(biz_type, biz_id)`

## 6. 支撑表设计

### 6.1 `statistics_daily`

建议字段：

- `stat_id`
- `stat_date`
- `auction_count`
- `sold_count`
- `unsold_count`
- `bid_count`
- `gmv_amount`
- `created_at`
- `updated_at`

约束与索引：

- `uk_statistics_daily_date(stat_date)`

### 6.2 `task_log`

建议字段：

- `task_log_id`
- `task_type`
- `biz_key`
- `task_status`
- `retry_count`
- `last_error`
- `scheduled_at`
- `started_at`
- `finished_at`
- `created_at`

状态建议：

- `task_status`：`PENDING`、`RUNNING`、`SUCCESS`、`FAILED`、`RETRYING`、`DEAD`

索引建议：

- `idx_task_log_type_status(task_type, task_status)`
- `idx_task_log_biz_key(biz_key)`

### 6.3 `operation_log`

建议字段：

- `operation_log_id`
- `operator_id`
- `operator_role`
- `module_name`
- `operation_name`
- `biz_key`
- `result`
- `detail`
- `created_at`

索引建议：

- `idx_operation_log_operator_created(operator_id, created_at)`
- `idx_operation_log_module_created(module_name, created_at)`

## 7. 关键约束汇总

必须明确以下业务约束：

- `order_info.auction_id` 全局唯一，确保一场拍卖最多只生成一笔订单
- `payment_record.transaction_no` 全局唯一，确保第三方流水不重复入账
- `bid_record(auction_id, bidder_id, request_id)` 唯一，控制客户端重复提交
- `review(order_id, reviewer_id)` 唯一，防止重复评价
- `item_image(item_id, sort_no)` 唯一，防止同一拍品图片顺序冲突

必须通过事务或应用层控制的约束：

- 同一拍卖任一时刻只能存在一个合法当前最高价
- 出价记录写入、最高价更新、最高出价者更新、延时保护更新必须同事务提交
- 拍卖结束后不得再接受有效出价
- 同一拍品同时只能存在一个非终态拍卖活动

## 8. 索引策略汇总

高优先级索引如下：

- `auction(status, start_time, end_time)`
- `bid_record(auction_id, created_at)`
- `bid_record(auction_id, bidder_id, created_at)`
- `order_info(buyer_id, order_status)`
- `payment_record(order_id, pay_status)`
- `payment_record(transaction_no)`
- `notification(user_id, read_status, created_at)`
- `item(seller_id, item_status, created_at)`

说明：

- 索引优先服务活动调度、出价历史、订单查询、支付回调幂等和通知中心
- 不建议在课程设计阶段过早增加过多组合索引，避免写放大和维护复杂度

## 9. 初始化脚本落地

`S03` 已实际落地以下脚本与验证入口：

```text
sql/
├── schema.sql
└── seed.sql

scripts/
├── db/
│   └── setup_local_mysql.sh
└── test_db.sh
```

各文件职责如下：

- `sql/schema.sql`：创建 15 张核心表，并以内联方式落地主键、唯一约束、外键和关键索引
- `sql/seed.sql`：插入管理员、客服、演示买家、演示卖家和 4 个基础分类，脚本可重复执行
- `scripts/db/setup_local_mysql.sh`：在仓库 `build/test_mysql/` 下初始化并启动用户态本地 MySQL，随后自动导入 `schema.sql` 和 `seed.sql`
- `scripts/test_db.sh`：生成测试配置，执行 `auction_app --check-db` 和数据库冒烟测试二进制

当前脚本约定如下：

- `schema.sql` 使用 `CREATE TABLE IF NOT EXISTS`，适合课程设计阶段的重复初始化
- 索引和约束直接内联在建表语句中，避免额外的索引脚本管理复杂度
- `seed.sql` 使用 `ON DUPLICATE KEY UPDATE`，保证基础数据重复导入时保持幂等
- 本地验证优先走 Unix socket，规避实验环境下的 TCP 端口和沙箱限制

## 10. 初始化数据基线

当前已经落地的基础种子数据包括：

- 1 个管理员账号：`admin`
- 1 个客服账号：`support`
- 1 个演示卖家账号：`seller_demo`
- 1 个演示买家账号：`buyer_demo`
- 4 个基础拍品分类：数码设备、图书文创、运动户外、校园收藏

说明：

- 密码字段使用 `SHA-512 crypt` 风格摘要，避免把明文密码写入数据库
- `seed.sql` 当前只保留基础账号与分类，不提前注入拍卖、订单、支付等交易数据
- 后续如需演示完整业务链路，可在 `S04-S08` 完成后追加演示数据脚本

## 11. 应用接入基线

为支撑后续模块直接接库，`S03` 已补齐以下应用侧能力：

- 配置项扩展：`mysql.socket`、`mysql.charset`、`mysql.connect_timeout_seconds`
- 连接封装：
  - [mysql_connection.h](/home/ljh/project/soft_course_design/src/common/db/mysql_connection.h)
  - [mysql_connection.cpp](/home/ljh/project/soft_course_design/src/common/db/mysql_connection.cpp)
- Repository 基类：
  - [base_repository.h](/home/ljh/project/soft_course_design/src/repository/base_repository.h)
- 数据库元信息仓储：
  - [database_meta_repository.h](/home/ljh/project/soft_course_design/src/repository/database_meta_repository.h)
  - [database_meta_repository.cpp](/home/ljh/project/soft_course_design/src/repository/database_meta_repository.cpp)
- 应用层数据库健康检查：
  - [database_health_service.h](/home/ljh/project/soft_course_design/src/application/database_health_service.h)
  - [database_health_service.cpp](/home/ljh/project/soft_course_design/src/application/database_health_service.cpp)

当前基线能力如下：

- 可按 TCP 或 Unix socket 建立 MySQL 连接
- 已支持最小 SQL 执行、计数查询、字符串查询、事务起停和转义工具
- `auction_app --check-db` 可输出数据库状态 JSON，作为后续模块落地前的统一连库检查入口

## 12. 本步完成判定

`S03` 已满足以下完成条件：

- 15 张核心表、状态字段、唯一约束、外键和关键索引已在 `schema.sql` 中真实落地
- 基础账号和分类种子数据已在 `seed.sql` 中真实落地
- MySQL 连接配置、连接封装、Repository 基类和元信息查询仓储已进入 `src/`
- 已提供 `scripts/db/setup_local_mysql.sh` 和 `scripts/test_db.sh` 两个可复用验证脚本
- 已通过 `auction_app --check-db` 验证数据库可达、schema 完整、seed 完整
- 后续 `S04-S11` 可直接基于当前表结构和仓储基础继续开发

## 13. 本步验证结果

本步验证命令如下：

- `cmake -S . -B build`
- `cmake --build build`
- `ctest --test-dir build --output-on-failure -E auction_database_smoke`
- `ctest --test-dir build --output-on-failure -R auction_database_smoke`
- `AUCTION_APP_CONFIG=/home/ljh/project/soft_course_design/build/test_config/app.mysql.test.json ./build/bin/auction_app --check-db`

实际验证结果如下：

- `auction_smoke_tests`、`auction_check_config` 通过
- `auction_database_smoke` 通过
- `--check-db` 返回 `code=0`
- 数据库状态为：
  - `tableCount=15`
  - `requiredTableCount=15`
  - `adminUserCount=1`
  - `categoryCount=4`
  - `schemaReady=true`
  - `seedReady=true`

## 14. 本步交付结果

本步实际交付如下：

- 数据库设计文档：[数据库设计说明.md](/home/ljh/project/soft_course_design/docs/数据库设计说明.md)
- 数据库脚本：[schema.sql](/home/ljh/project/soft_course_design/sql/schema.sql)、[seed.sql](/home/ljh/project/soft_course_design/sql/seed.sql)
- 数据库封装代码：[mysql_connection.cpp](/home/ljh/project/soft_course_design/src/common/db/mysql_connection.cpp)、[base_repository.h](/home/ljh/project/soft_course_design/src/repository/base_repository.h)、[database_meta_repository.cpp](/home/ljh/project/soft_course_design/src/repository/database_meta_repository.cpp)
- 数据库验证脚本：[setup_local_mysql.sh](/home/ljh/project/soft_course_design/scripts/db/setup_local_mysql.sh)、[test_db.sh](/home/ljh/project/soft_course_design/scripts/test_db.sh)
- 进度更新文档：[schedule.md](/home/ljh/project/soft_course_design/docs/schedule.md)
