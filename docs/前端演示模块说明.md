# 在线拍卖平台前端演示模块说明

## 1. 文档目的

本文档说明本次新增的前端演示模块，用于课程设计答辩时以浏览器方式展示在线拍卖平台的核心业务闭环。

本模块定位为“演示台”，不是完整生产前端。它从演示数据库 `auction_demo` 读取事实数据，以可视化方式展示拍品、拍卖、出价、订单、支付、评价、通知、任务日志和统计日报。

## 2. 功能范围

新增功能如下：

- 浏览器入口：`GET /demo`
- 首页入口：`GET /`
- 静态资源：`GET /assets/demo/app.css`、`GET /assets/demo/app.js`
- 只读演示数据接口：`GET /api/demo/dashboard`
- 前端专项测试入口：`scripts/test.sh frontend`

前端页面覆盖以下演示内容：

- 主业务流程：拍品发布、审核、拍卖创建、出价、结算、支付、履约、评价。
- 角色说明：管理员、运维/客服、卖家、买家、竞争买家。
- 拍卖状态：已成交、竞拍中、已流拍、待开始。
- 交易闭环：订单、支付记录、双方评价。
- 高风险链路：并发出价、拍卖结束竞争、支付回调幂等、缓存降级、安全负向。
- 运维可观测：站内通知、任务日志、操作日志、统计日报。

## 3. 实现位置

前端资源：

- `assets/demo/index.html`
- `assets/demo/app.css`
- `assets/demo/app.js`

后端只读接口：

- `src/access/http/demo_http.h`
- `src/access/http/demo_http.cpp`
- `src/application/demo_dashboard_service.h`
- `src/application/demo_dashboard_service.cpp`

测试与脚本：

- `tests/demo/demo_dashboard_tests.cpp`
- `scripts/test_demo_frontend.sh`
- `scripts/test.sh frontend`

## 4. 接口说明

### 4.1 `GET /demo`

返回前端演示页面。

页面会自动调用 `/api/demo/dashboard` 加载演示数据。如果接口失败，页面会提示先执行演示环境初始化脚本。

### 4.2 `GET /api/demo/dashboard`

返回统一响应结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "view": "demo_dashboard",
    "mode": "read_only_demo",
    "summary": {},
    "accounts": [],
    "auctions": [],
    "bids": [],
    "orders": [],
    "payments": [],
    "reviews": [],
    "notifications": [],
    "taskLogs": [],
    "operationLogs": [],
    "statisticsDaily": [],
    "riskScenarios": []
  }
}
```

主要字段说明：

| 字段 | 说明 |
|---|---|
| `summary` | 演示账号数、拍品数、拍卖数、出价数、订单数和成交金额 |
| `accounts` | 演示账号、角色、状态和答辩用途 |
| `auctions` | 拍卖活动、当前最高价、最高出价者、延时保护配置和状态 |
| `bids` | 最近出价记录 |
| `orders` | 演示成交订单 |
| `payments` | mock 支付记录 |
| `reviews` | 买卖双方评价 |
| `notifications` | 站内通知 |
| `taskLogs` | 调度任务日志 |
| `operationLogs` | 管理操作日志 |
| `statisticsDaily` | 统计日报 |
| `riskScenarios` | 高风险测试说明卡片 |

## 5. 数据来源

本模块只读取 MySQL，不写入交易状态。默认数据来源如下：

- 数据库：`auction_demo`
- 初始化脚本：`scripts/deploy/init_demo_env.sh`
- 演示数据：`sql/demo_data.sql`

这种设计保证演示页面展示的内容来自数据库事实，同时避免前端演示误改核心拍卖、订单和支付状态。

## 6. 运行方式

初始化演示环境：

```bash
./scripts/deploy/init_demo_env.sh
```

启动服务：

```bash
./scripts/deploy/run_demo_server.sh
```

浏览器打开：

```text
http://127.0.0.1:18080/demo
```

接口验证：

```bash
curl http://127.0.0.1:18080/api/demo/dashboard
```

## 7. 验证方式

前端演示数据专项验证：

```bash
scripts/test.sh frontend
```

该入口会：

- 构建工程。
- 初始化 `auction_demo`。
- 导入 `sql/demo_data.sql`。
- 执行 `auction_demo_dashboard_tests`，校验演示接口返回账号、拍品、拍卖、订单、支付和高风险说明。

全量验证仍使用：

```bash
ctest --test-dir build --output-on-failure
```

## 8. 边界说明

- 本模块是只读演示台，不提供真实出价、支付、审核等写操作入口。
- 核心交易写路径仍以服务层实现和自动化测试为准。
- 若浏览器访问 `/api/...` 其他业务路径返回 `404 Not Found`，说明该业务 HTTP 控制器尚未批量接入，不代表服务层功能缺失。
- 若 `/demo` 页面能打开但数据加载失败，优先检查是否已执行 `scripts/deploy/init_demo_env.sh`，以及演示服务是否使用 `build/demo_config/app.demo.json` 启动。
