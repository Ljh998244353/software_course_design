# 在线拍卖平台测试计划与用例说明

## 1. 文档目的

本文档用于记录 `S13` 自动化测试基线和 `S14` 高风险专项测试的真实落地结果，统一说明当前仓库的测试分层、执行入口、覆盖边界和后续专项测试衔接方式。

`S13` 的目标是基于现有工程做统一收口，不重复引入与当前代码实现不匹配的空测试骨架。

## 2. 当前测试基线

当前仓库的自动化测试基线如下：

- 使用 `CTest` 作为统一注册、筛选和执行入口。
- 测试程序主体采用“独立测试二进制 + `assert` 断言”的方式，而不是额外平移到新的测试框架。
- 所有依赖数据库的测试继续复用 `scripts/db/setup_local_mysql.sh` 和 `scripts/db/write_test_config.sh`。
- `scripts/test_*.sh` 负责单模块或专项测试的数据库准备与测试启动。
- `scripts/test.sh` 作为统一总入口，按测试分层或模块标签调用 `ctest`。
- `auction_http_baseline` 在 `CTest` 中设置 `TIMEOUT 600`，避免真实 HTTP 服务启动或请求阻塞时影响整轮回归收敛。

需要说明：

- 课程计划中曾写到 `GoogleTest`，但当前代码现实并未采用该框架。
- 本步以“统一现有测试基线”为目标，因此保留已稳定运行的 `CTest + 断言式测试二进制 + Shell 脚本` 方案。

## 3. 目录与分层

当前测试目录按以下方式组织：

| 分层 | 代码位置 | 说明 |
|---|---|---|
| 单元/冒烟测试 | `tests/unit/smoke_tests.cpp` | 校验配置模板、统一响应、错误码和健康检查基础能力 |
| 数据库冒烟测试 | `tests/integration/database_smoke_tests.cpp` | 校验 schema、seed、应用连库和 `--check-db` |
| 模块流测试 | `tests/auth/` `tests/item/` `tests/auction/` `tests/bid/` `tests/order/` `tests/payment/` `tests/review/` `tests/statistics/` `tests/ops/` | 每个模块各自验证最小业务闭环 |
| 主流程联调/契约测试 | `tests/integration/full_flow_tests.cpp` | 覆盖认证、拍品、审核、拍卖、竞价、订单、支付、履约、评价、统计、运维的服务层联调闭环 |
| 真实 HTTP 基线测试 | `tests/http/` `scripts/test_http.sh` | 覆盖 `/app`、认证会话接口、管理员状态接口、卖家拍品接口、管理员审核与管理端拍卖接口、公开拍卖、真实出价、通知轮询、订单支付、履约、评价、通知中心、信用摘要、统计报表、运维异常、`/api/system/context` 和统一 JSON `404` 的真实 HTTP 冒烟验证；S27 已补齐失败诊断、运行前响应文件清理、分阶段输出和支付坏签名回调状态保护断言 |
| 高风险专项测试 | `tests/stress/high_risk_flow_tests.cpp` | 覆盖并发出价、拍卖结束竞争、结算建单竞争、支付回调并发幂等、缓存降级和通知失败重试 |
| 安全负向测试 | `tests/security/security_negative_tests.cpp` | 覆盖未认证、RBAC 越权、冻结账号、非法金额、幂等键冲突和支付回调篡改 |
| 前端演示数据测试 | `tests/demo/demo_dashboard_tests.cpp` | 校验答辩演示台所需账号、拍品、拍卖、订单、支付和高风险说明可从 `auction_demo` 聚合返回 |
| 测试脚本 | `scripts/test.sh` `scripts/test_*.sh` | 统一入口与分模块入口 |

`S14` 已补齐 `tests/stress/` 和 `tests/security/`，两类专项测试均接入 `CTest` 和 `scripts/test.sh`。

## 4. CTest 标签与执行入口

`S13` 新增并统一了以下 `CTest` 标签：

| 标签 | 作用 |
|---|---|
| `suite_smoke` | 运行不依赖 MySQL 的基础冒烟测试 |
| `suite_db` | 运行数据库冒烟测试 |
| `suite_module` | 运行各业务模块最小闭环测试 |
| `suite_integration` | 运行跨模块集成验证 |
| `suite_contract` | 运行当前接口契约映射验证，现阶段对应主流程联调 |
| `suite_http` | 运行真实 HTTP 基线验证 |
| `suite_risk` | 运行 S14 高风险专项和安全负向测试 |
| `suite_stress` | 运行并发、竞争、幂等、降级和通知失败专项测试 |
| `suite_security` | 运行认证、权限、输入校验和支付回调安全负向测试 |
| `suite_demo` | 运行前端演示数据聚合测试 |
| `requires_mysql` | 标记依赖本地测试 MySQL 的用例 |
| `module_auth` 等模块标签 | 支持按业务模块单独执行 |

统一执行入口如下：

```bash
scripts/test.sh
scripts/test.sh smoke
scripts/test.sh db
scripts/test.sh module
scripts/test.sh integration
scripts/test.sh contract
scripts/test.sh http
scripts/test.sh risk
scripts/test.sh stress
scripts/test.sh security
scripts/test.sh frontend
scripts/test.sh auth
scripts/test.sh list
```

`scripts/test.sh` 内部会先执行 `scripts/bootstrap.sh`，再通过 `ctest` 和标签筛选执行对应测试集。

如需直接使用 `ctest`，当前支持的常用方式如下：

```bash
ctest --test-dir build --output-on-failure
ctest --test-dir build --output-on-failure -L suite_module
ctest --test-dir build --output-on-failure -L suite_contract
ctest --test-dir build --output-on-failure -L suite_http
ctest --test-dir build --output-on-failure -L suite_risk
ctest --test-dir build --output-on-failure -L module_bid
```

## 5. 共享 MySQL 运行时约束

当前大部分业务测试都依赖 `build/test_mysql` 下的本地 MySQL 运行时目录。

为避免 `ctest -j` 或按标签回归时多个测试同时竞争同一套 `datadir`、`socket` 和 `ibdata1` 文件，`S13` 在 `CMakeLists.txt` 中为所有依赖数据库的用例统一增加了：

- `requires_mysql` 标签
- `RESOURCE_LOCK auction_mysql_test_runtime`

这样即使使用并行 `ctest`，依赖同一本地 MySQL 的测试也会按资源锁顺序执行，减少互相踩库导致的伪失败。

## 6. 覆盖范围

当前自动化测试已经覆盖以下内容：

- 工程配置加载、统一响应模型、错误码与健康检查。
- 数据库 schema、seed、基础连库验证。
- 认证、物品审核、拍卖管理、竞价通知、订单支付、评价、统计、运维各模块最小闭环。
- 支付后履约、互评、统计重建和运维查询的主流程联调。
- 当前服务层实现所对应的接口契约映射验证。
- 并发出价下最高价唯一、竞价结束后拒绝晚到出价、结算建单竞争下单拍卖单订单。
- 支付成功回调并发重复到达时只产生一次有效状态变更，失败回调在成功后保持幂等忽略。
- 竞价缓存刷新失败不回滚数据库事实，通知直推失败可持久化并通过重试补偿。
- 未认证、越权、冻结账号、非法出价金额、幂等键冲突、支付回调商户号/金额/签名/流水篡改等安全负向路径。
- 前端演示台 `/api/demo/dashboard` 所需数据聚合能力，确保浏览器演示能读取 `auction_demo` 中的账号、拍卖、交易和高风险说明。
- `S17-S27` 的真实 HTTP 接入基线，包括 `/app`、注册、登录、当前用户、登出、管理员状态变更、卖家拍品创建编辑、图片元数据、我的拍品、拍品详情、提交审核、管理员审核、审核日志、管理端拍卖创建/查询/修改/取消、公开拍卖列表/详情/当前最高价/出价历史、真实出价、重复幂等键、低价、过期、冻结用户、个人出价状态、被超越通知轮询和已读、订单列表、订单详情、支付发起、支付状态、支付坏签名回调拒绝且订单仍待支付、支付成功回调、重复回调幂等、卖家发货、买家确认收货、订单评价、重复评价拒绝、信用摘要、评价通知、通知中心、统计日报重跑/查询/导出、运维日志、任务日志、异常看板、异常标记、补偿入口、通知重试、全局状态徽标、忙碌态样式、HTML5 表单约束、角色越权提示、管理员专属禁用提示、`GET /api/system/context` 和未注册 `/api/...` 的统一 JSON `404`。
- `scripts/test_http.sh` 会在运行前清理 `build/test_http` 下旧响应文件，失败时输出当前阶段、失败行、最近响应文件和服务端日志尾部，便于定位真实 HTTP 回归问题。

当前明确未覆盖或只做最小覆盖的内容：

- Drogon 真实 HTTP 的完整业务回归测试，目前已完成认证、角色路由、卖家拍品、管理员审核、管理端拍卖、公开拍卖、真实出价、通知轮询、订单支付、履约、评价、通知中心、信用展示、统计报表、运维异常、全站 UX、安全前端约束和 S27 自动化回归收口。
- 200 并发在线验证与性能测试。

200 并发在线验证继续留到后续最终交付阶段。

## 7. 用例与入口对应关系

| 测试名称 | 入口脚本/命令 | 覆盖重点 |
|---|---|---|
| `auction_smoke_tests` | `scripts/test.sh smoke` | 配置模板、错误码、统一响应、健康检查 |
| `auction_check_config` | `scripts/test.sh smoke` | 应用配置加载 |
| `auction_database_smoke` | `scripts/test.sh db` | schema、seed、应用连库、自检 |
| `auction_auth_flow` | `scripts/test.sh auth` | 注册、登录、Token、RBAC、账号状态 |
| `auction_item_flow` | `scripts/test.sh item` | 草稿、图片、提审、审核流转 |
| `auction_auction_flow` | `scripts/test.sh auction` | 活动创建、开始/结束调度、状态机 |
| `auction_bid_flow` | `scripts/test.sh bid` | 出价校验、幂等键、延时保护、通知旁路 |
| `auction_order_flow` | `scripts/test.sh order` | 建单与超时关闭 |
| `auction_payment_flow` | `scripts/test.sh payment` | 支付发起、验签、金额校验、幂等回调 |
| `auction_review_flow` | `scripts/test.sh review` | 互评约束、信用汇总 |
| `auction_statistics_flow` | `scripts/test.sh statistics` | 日报聚合、重跑、导出 |
| `auction_ops_flow` | `scripts/test.sh ops` | 运维查询、异常标记、补偿入口 |
| `auction_integration_flow` | `scripts/test.sh contract` | 主流程联调与当前接口契约映射 |
| `auction_http_baseline` | `scripts/test.sh http` | 完整前端业务入口、静态资源、注册、登录、当前用户、登出、管理员状态变更、卖家拍品管理、管理员审核、管理端拍卖、公开拍卖、真实出价、通知轮询、订单支付、支付坏签名拒绝和状态保护、履约、评价、通知中心、信用摘要、统计报表、运维异常、全站 UX 和安全前端约束、RBAC 越权拒绝、受保护测试接口和 JSON `404` 基线 |
| `auction_high_risk_flow` | `scripts/test.sh stress` | 并发出价、结束竞争、结算竞争、支付并发幂等、缓存降级、通知失败重试 |
| `auction_security_negative` | `scripts/test.sh security` | 未认证、越权、冻结账号、非法金额、幂等冲突、支付回调篡改 |
| `auction_demo_dashboard` | `scripts/test.sh frontend` | 前端演示台数据接口所需演示数据聚合 |

## 8. S13 验证结果

`S13` 实际执行并通过以下验证命令：

```bash
scripts/test.sh smoke
scripts/test.sh db
scripts/test.sh module
scripts/test.sh contract
ctest --test-dir build --output-on-failure
```

验证结论如下：

- 统一入口 `scripts/test.sh` 可按分层和模块执行。
- `CTest` 标签筛选已生效。
- MySQL 依赖测试已统一挂载资源锁。
- 当前全量 `ctest` 为 `13/13` 全部通过。

## 9. 后续衔接

`S14` 后当前全量验证结果：

- `scripts/test.sh risk` 为 `2/2` 通过。
- `ctest --test-dir build --output-on-failure` 在 `S14` 时为 `15/15` 通过。
- 补充前端演示台后，当前全量 `ctest --test-dir build --output-on-failure` 为 `16/16` 通过。
- 完成 `S17` 后，`scripts/test.sh http` 为 `1/1` 通过，全量 `ctest --test-dir build --output-on-failure` 更新为 `17/17` 通过。
- 完成 `S18` 后，`scripts/test.sh http` 仍为 `1/1` 通过，`auction_http_baseline` 覆盖范围扩展到注册、登录态恢复、登出、管理员状态变更和角色越权拒绝。
- 完成 `S19` 后，`scripts/test.sh http` 仍为 `1/1` 通过，`auction_http_baseline` 覆盖范围扩展到卖家拍品创建、非法字段、图片元数据、删除图片、编辑、列表、详情、提交审核和提审后禁止编辑；全量 `ctest` 为 `17/17` 通过。
- 完成 `S20` 后，`scripts/test.sh http` 仍为 `1/1` 通过，`auction_http_baseline` 覆盖范围扩展到管理员待审列表、审核通过、卖家查看审核结果、创建拍卖、管理端列表、详情、修改、取消、审核驳回、卖家查看驳回原因和审核日志；全量 `ctest` 为 `17/17` 通过。
- 完成 `S21` 后，`scripts/test.sh http` 仍为 `1/1` 通过，`auction_http_baseline` 覆盖范围扩展到公开拍卖列表、公开详情、当前最高价、出价历史和非法状态筛选；全量 `ctest` 为 `17/17` 通过。
- 完成 `S22` 后，`scripts/test.sh http` 仍为 `1/1` 通过，`auction_http_baseline` 覆盖范围扩展到真实出价、重复幂等键、幂等冲突、低价、个人出价状态、价格刷新、出价历史状态更新、被超越通知轮询、通知已读、过期活动拒绝和冻结用户拒绝；全量 `ctest` 为 `17/17` 通过。
- 完成 `S23` 后，`scripts/test.sh http` 仍为 `1/1` 通过，`auction_http_baseline` 覆盖范围扩展到订单列表、订单详情、支付发起、重复支付复用、支付状态查询、支付成功回调、重复回调幂等、卖家发货、买家确认收货、越权访问和非法状态拒绝；全量 `ctest` 为 `17/17` 通过。
- 完成 `S24` 后，`scripts/test_http.sh` 通过，`auction_http_baseline` 覆盖范围扩展到订单评价空列表、买家评价、重复评价拒绝、卖家信用摘要、非订单参与者查询拒绝、评价通知、通知已读、卖家评价、订单状态推进到 `REVIEWED`、订单互评查询和买家信用摘要；全量 `ctest` 为 `17/17` 通过。
- 完成 `S25` 后，`scripts/test_http.sh` 通过，`auction_http_baseline` 覆盖范围扩展到管理员日报重跑、日报查询、CSV 导出、客服统计查询拒绝、客服日志查询、异常看板、手工标记异常、管理员统计补偿、客服通知重试拒绝和管理员通知重试；全量 `ctest` 为 `17/17` 通过。
- 完成 `S26` 后，`scripts/test_http.sh` 通过，`auction_http_baseline` 覆盖范围扩展到全局状态徽标、忙碌态样式、HTML5 表单约束、角色越权提示、管理员专属禁用提示、拍卖时间校验文案、管理员补偿和通知重试前端限制文案；全量 `ctest` 为 `17/17` 通过。
- 完成 `S27` 后，`scripts/test_http.sh` 在原有主链路上补齐支付坏签名回调拒绝且订单/支付状态不推进断言，并新增阶段输出、运行前临时响应清理、失败时最近响应和服务日志尾部输出；`auction_http_baseline` 在 `CTest` 中增加 600 秒超时，仍可通过 `scripts/test.sh http` 和全量 `ctest` 一键执行。

`S14` 完成后，后续测试工作按以下方向继续：

- `S15`：在专项测试稳定后执行最终全量回归与演示环境验证。

补充前端演示台后，新增验证入口：

- `scripts/test.sh frontend`：初始化 `auction_demo` 并执行 `auction_demo_dashboard`。
- 全量 `ctest` 会包含前端演示数据测试。

完成 `S17-S27` 后，新增并扩展验证入口：

- `scripts/test.sh http`：启动真实 HTTP 服务，验证 `/app`、`/app/`、`/assets/app/app.css`、`/assets/app/app.js`、`/api/auth/register`、`/api/auth/login`、`/api/auth/me`、`/api/auth/logout`、`/api/admin/users/{id}/status`、`/api/items`、`/api/items/mine`、`/api/items/{id}`、`/api/items/{id}/images`、`/api/items/{id}/submit-audit`、`/api/admin/items/pending`、`/api/admin/items/{id}/audit`、`/api/admin/items/{id}/audit-logs`、`/api/admin/auctions`、`/api/admin/auctions/{id}`、`/api/admin/auctions/{id}/cancel`、`/api/auctions`、`/api/auctions/{id}`、`/api/auctions/{id}/price`、`/api/auctions/{id}/bids`、`/api/auctions/{id}/my-bid`、`POST /api/auctions/{id}/bids`、`/api/notifications`、`PATCH /api/notifications/{id}/read`、`/api/orders/mine`、`/api/orders/{id}`、`POST /api/orders/{id}/pay`、`/api/orders/{id}/payment`、`POST /api/payments/callback` 的坏签名拒绝、成功回调和重复幂等、`POST /api/orders/{id}/ship`、`POST /api/orders/{id}/confirm-receipt`、`POST /api/reviews`、`GET /api/orders/{id}/reviews`、`GET /api/users/{id}/reviews/summary`、`GET /api/admin/statistics/daily`、`POST /api/admin/statistics/daily/rebuild`、`GET /api/admin/statistics/daily/export`、`GET /api/admin/ops/operation-logs`、`GET /api/admin/ops/task-logs`、`GET /api/admin/ops/exceptions`、`POST /api/admin/ops/exceptions/mark`、`POST /api/admin/ops/notifications/retry`、`POST /api/admin/ops/compensations`、`/api/system/context`、统一 JSON `404`，以及 S26 静态前端约束断言。
- 全量 `ctest` 会包含 `auction_http_baseline`。
